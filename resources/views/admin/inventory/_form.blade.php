@csrf
<div class="row g-3"><div class="col-md-6"><label class="form-label">Product</label><select name="product_id" class="form-select" required>@foreach($products as $product)<option value="{{ $product->id }}" @selected(old('product_id',$inventory->product_id)===$product->id)>{{ $product->name }} ({{ $product->sku }})</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Warehouse</label><input name="warehouse" value="{{ old('warehouse',$inventory->warehouse ?? 'main') }}" class="form-control" required></div>@foreach(['available_qty'=>'Available','reserved_qty'=>'Reserved','damaged_qty'=>'Damaged','minimum_stock'=>'Minimum','reorder_level'=>'Reorder','maximum_stock'=>'Maximum'] as $field=>$label)<div class="col-md-2"><label class="form-label">{{ $label }}</label><input type="number" min="0" name="{{ $field }}" value="{{ old($field,$inventory->{$field} ?? 0) }}" class="form-control"></div>@endforeach</div><div class="mt-3"><button class="btn btn-primary">Save</button><a href="{{ route('admin.inventory.index') }}" class="btn btn-light">Cancel</a></div>
