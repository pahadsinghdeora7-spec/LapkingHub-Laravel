<x-layouts.admin title="Inventory | LapkingHub" page-title="Inventory">
<form class="row g-2 mb-3"><div class="col-md-4"><input name="search" value="{{ $filters['search'] ?? '' }}" class="form-control" placeholder="Search product, SKU, warehouse"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? '')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Search</button></div><div class="col-md-3 text-end"><a href="{{ route('admin.inventory.low-stock') }}" class="btn btn-warning">Low Stock</a> <a href="{{ route('admin.inventory.create') }}" class="btn btn-success">Add</a></div></form>
<div class="card shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Product</th><th>Warehouse</th><th>Available</th><th>Reserved</th><th>Damaged</th><th>Reorder</th><th>Status</th><th></th></tr></thead><tbody>@forelse($inventories as $inventory)<tr><td><strong>{{ $inventory->product?->name }}</strong><div class="small text-secondary">{{ $inventory->product?->sku }}</div></td><td>{{ $inventory->warehouse }}</td><td>{{ $inventory->available_qty }}</td><td>{{ $inventory->reserved_qty }}</td><td>{{ $inventory->damaged_qty }}</td><td>{{ $inventory->reorder_level }}</td><td><span class="badge text-bg-{{ $inventory->status === 'in_stock' ? 'success' : ($inventory->status === 'low_stock' ? 'warning' : 'danger') }}">{{ str($inventory->status)->replace('_',' ')->title() }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.inventory.show',$inventory) }}">Manage</a></td></tr>@empty<tr><td colspan="8" class="text-center text-secondary py-4">No inventory records found.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $inventories->links() }}</div></div>
</x-layouts.admin>
