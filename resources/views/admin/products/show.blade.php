<x-layouts.admin title="Product Details | LapkingHub" page-title="{{ $product->name }}"><div class="card shadow-sm"><div class="card-header d-flex justify-content-between"><span>{{ $product->sku }}</span><a href="{{ route('admin.products.edit', $product) }}" class="btn btn-sm btn-primary">Edit</a></div><div class="card-body"><div class="row g-3 mb-4"><div class="col-md-3"><div class="border rounded p-2 text-center"><img src="{{ $product->primaryImage?->url ?? ($product->images->first()?->url ?? '') }}" class="img-fluid rounded bg-light" style="max-height:180px" alt="{{ $product->name }}" onerror="this.style.display='none'"><div class="small text-secondary mt-2">Primary Image</div></div></div><div class="col-md-9"><div class="d-flex flex-wrap gap-2 mb-2">@foreach($product->images as $image)<img src="{{ $image->url }}" class="rounded border" style="width:72px;height:72px;object-fit:cover" alt="{{ $image->alt_text }}">@endforeach</div><div class="badge text-bg-info">{{ $product->images->count() }} gallery image(s)</div> <a href="{{ route('admin.products.images.index', $product) }}" class="btn btn-sm btn-outline-primary ms-2">Manage Gallery</a></div></div><dl class="row"><dt class="col-md-3">Brand</dt><dd class="col-md-9">{{ $product->brand?->name ?? '—' }}</dd><dt class="col-md-3">Category</dt><dd class="col-md-9">{{ $product->category?->name }}</dd><dt class="col-md-3">OEM Part</dt><dd class="col-md-9">{{ $product->oem_part_number ?: '—' }}</dd><dt class="col-md-3">Alternate Parts</dt><dd class="col-md-9">{{ $product->alternatePartNumbers->pluck('part_number')->implode(', ') ?: '—' }}</dd><dt class="col-md-3">Pricing</dt><dd class="col-md-9">MRP ₹{{ $product->mrp }} · Selling ₹{{ $product->price }} · Cost ₹{{ $product->cost_price }}</dd><dt class="col-md-3">Compatibility</dt><dd class="col-md-9">{{ $product->laptopModels->pluck('name')->implode(', ') ?: '—' }}</dd><dt class="col-md-3">Status</dt><dd class="col-md-9">{{ ucfirst($product->status) }} / {{ str($product->stock_status)->replace('_',' ')->title() }}</dd><dt class="col-md-3">Descriptions</dt><dd class="col-md-9"><strong>{{ $product->short_description }}</strong><div>{{ $product->description }}</div></dd></dl></div></div></x-layouts.admin>
